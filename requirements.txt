streamlit>=1.45,<2.0
requests>=2.32,<3.0
faiss-cpu>=1.8,<2.0
PyMuPDF>=1.24,<2.0
dashscope>=1.20,<2.0
python-dotenv>=1.0,<2.0
numpy>=2.0,<3.0
fastembed>=0.3,<1.0

# fastembed 隐式依赖，缺失会导致运行报 No module named 'pandas'
pandas>=2.0,<3.0

# 健康数据落盘加密（storage_io 的 Fernet 对称加密，密钥见 DATA_ENC_KEY）
cryptography>=42,<46

# 测试框架（tests/ 下的 pytest 用例）
pytest>=8,<9
